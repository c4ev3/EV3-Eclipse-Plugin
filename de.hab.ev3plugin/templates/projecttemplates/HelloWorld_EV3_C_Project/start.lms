// most used paths: /media/card, /media/usb and ../prjs/BrkProg_SAVE for internal Memory
define destdir '$(uploadDir)'
define starter 'myapps/${projctName}Starter.rbf'
define elfexec '../${projectName}.elf'
// don't edit the above, unless ye knowin what ye doin

// stolen from http://www.robotnav.com/run-programs-without-network/
vmthread MAIN
{
    DATA32 Timer
    DATA32 Status
    SYSTEM(elfexec, Status)
    TIMER_WAIT(1000,Timer)
    TIMER_READY(Timer)
}
